{
  "name": "js-es6-es2017",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "js": "babel src -d dist --presets env",
    "js:watch": "babel src -d dist --presets env -w",
    "css": "node-sass src/styles -o dist/styles",
    "css:watch": "npm run css && node-sass src/styles -wo dist/styles",
    "start": "npm run js:watch | npm run css:watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "node-sass": "^4.9.0"
  }
}
